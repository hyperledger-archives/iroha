include "key.fbs";
include "commands.fbs";
include "primitives.fbs";

namespace iroha.api;

table Transaction {
  creatorPubKey:   PublicKey (required);
  command:         Command;
  signatures:      [Signature];
  hash:            [ubyte];
  message:         string;
}
