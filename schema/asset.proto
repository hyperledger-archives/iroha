syntax = "proto3";
package iroha.protocol; // I think protocol is better, by mizuki

// For Add/Subtract/Transfer
message Asset{
  string asset_id   = 1;
  int64  amount     = 2;
  int32  precision  = 3;
  string data       = 4;
}